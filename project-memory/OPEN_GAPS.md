# ESTIA · פערים פתוחים

> כל פער ידוע, ממוספר ומדורג. **פער שלא נרשם כאן אינו חוב — הוא באג
> שטרם התגלה.**
>
> נבדק מול עץ העבודה ב-27.8.2026, 20:45.
> **אומת מחדש מול עץ העבודה ומול המסד החי ב-6.9.2026** — ראה בלוק האימות למטה.

**סה"כ פערים רשומים: 32** · CRITICAL: 1 · HIGH: 6 · MEDIUM: 9 · LOW: 6 (כולל שארית) · **נסגרו: 10**

---

## אימות 6.9.2026, 22:45 — הסיבה ל-G-023 נמצאה

> **G-023 לא היה "אף אחד לא ניסה". ההרשמה לא יכלה לרוץ.**
>
> `signup.ts` דורש `DATABASE_URL` או `SUPABASE_SERVICE_ROLE_KEY` — יצירת
> הארגון הראשון היא הכתיבה היחידה שאף מדיניות RLS אינה יכולה לאשר, כי
> `organization_id in (select my_organizations())` ריק בהגדרה עבור מי שעדיין
> אינו חבר בכלום. **ב-`.env.local` אין אף אחד משניהם.** הקוד אומר את זה
> בעצמו: `SignupUnavailableError`, שורה 114.
>
> זה מסביר את המסד החי במדויק: 2 משתמשים · 0 ארגונים · 0 חברויות · 0 נכסים ·
> 0 הזמנות. **אף מסך במוצר לא הציג מעולם שורה אמיתית**, כי הצעד הראשון לא
> יכול היה לרוץ.
>
> **מה נעשה:** `supabase/migrations/0064_first_workspace.sql` מעביר את
> הכתיבה לפונקציה אחת ב-`SECURITY DEFINER` שיוצרת ארגון עבור `auth.uid()`
> **ואינה מקבלת מזהה משתמש בכלל** — היא לא יכולה לנקוב בשם אף אחד מלבד
> הקורא לה. ההרשמה כבר אינה דורשת שום סוד. הפתרון החלופי — להוסיף את המפתח
> — היה מתקן את הסימפטום ומשאיר מוצר שהתקנתו דורשת החזקת credential שקורא
> וכותב את הנתונים של כל הדיירים, לצורך insert אחד פעם בלקוח.
>
> ⚠️ **המיגרציה נכתבה ולא הוחלה** — ראה [`G-031`](#g-031). עד שתוחל,
> `create_first_workspace` אינו קיים במסד ו-G-023 נשאר פתוח בדיוק כפי שהיה.
>
> **נסגר באימות הזה:** [`G-021`](#g-021) — ארבעת המודולים נבנו.

> ⚠️ **המצב זז מהר מאוד.** בין תחילת הכתיבה לסיומה נחתו חמש מיגרציות,
> RLS, ספריית Supabase, שכבת שירות, נתיב Audit, מסכי אימות, CI ו-490
> בדיקות נוספות. **אמת מול המאגר לפני שאתה פועל לפי רשומה כאן.**
>
> **עדכון 20:52 — פערים שנסגרו אחרי כתיבת הרשומות שלהם:**
> [`G-003`](#g-003) (נחתו `.github/workflows/ci.yml` ו-`security.yml`) ·
> [`G-005`](#g-005) (נחתו `error.tsx` · `loading.tsx` · `not-found.tsx`) ·
> [`G-019`](#g-019) (נחת `src/lib/audit/pipeline.ts` עם בדיקות).
> נוספה גם שכבת שירות מלאה ופתרון `Actor` מ-session.
> **הרשומות עצמן נשארו כפי שנכתבו — הן תיעוד של רגע, לא של היום.**

---

## אימות 6.9.2026

> המרשם הזה מנה חמישה פערים שנסגרו מזמן. מרשם שמונה פער שנסגר הוא בדיוק
> כמו checker שמדווח "לא יכולתי לרוץ" ונקרא כ"לא מצאתי כלום" — שניהם
> נראים כמו סטטוס, ושניהם אינם.

כל רשומה נבדקה מול עץ העבודה ומול המסד החי (`shcauvnepnaemmnvahgy`).
**גוף הרשומות לא נגעתי בו** — הן תיעוד של רגע, כפי שקובעת הכותרת. מה
שהשתנה הוא שורת המצב בכותרת, ובלוק `אומת 6.9.2026` בראש כל רשומה שזזה.

**נסגרו באימות הזה (5):**

| פער   | מה הוכיח את הסגירה                                                         |
| ----- | -------------------------------------------------------------------------- |
| G-001 | 59 מיגרציות רשומות כמוחלות; הסכימה נבדקה טבלה־טבלה מול הקבצים              |
| G-011 | `npm run typecheck` נקי, והוא שלב שני מתוך שמונה ב-`npm run gate`          |
| G-019 | `src/lib/audit/pipeline.ts` + בדיקות; `defineOperation` מחייב רשומת audit  |
| G-020 | שלושת המפתחות הזרים קיימים; 685 FK בסכימה; מערכי scope שמורים בשני טריגרים |
| G-005 | 44 קבצי `loading.tsx`, גבול שגיאה שורשי ו-2 `not-found` — עם שארית, למטה   |

**לא נסגרו, בניגוד למה שאפשר היה לחשוב:**

- **G-003 (CI)** — שלושה workflows קיימים ומקומיטים (`ci.yml`, `database.yml`,
  `security.yml`), אבל **לא ניתן היה לאמת מכאן שהצנרת רצה ולו פעם אחת**: אין
  `gh` מותקן והמאגר פרטי. לפי הכלל של המרשם הזה עצמו — _פער נסגר כשראו אותו
  עובד, לא כשכתבו את הקוד שלו_ — הוא **נשאר פתוח**, ורק יורד ל-MEDIUM כי הקוד
  קיים והשער שהוא מריץ עובר מקומית.
- **G-004 (E2E)** — `e2e/` קיימת ומריצה סריקת מסלול × פרסונה על שמונה פרסונות,
  plus `crawl-links` · `dead-links` · `privacy`. זה **לא** E2E של מסע משתמש:
  אף בדיקה לא מבצעת הזמנה מקצה לקצה. צומצם ל-MEDIUM, לא נסגר.

**נוספו שבעה פערים שהמרשם לא הכיר (G-021 עד G-027).** רובם התגלו
בהרצת הגייט המלא ובבדיקת המסד החי, ואחד מהם — G-022 — חוסם את קונסולת
הפלטפורמה כולה.

---

## דירוג החומרה

|              | מתי                                        |
| ------------ | ------------------------------------------ |
| **CRITICAL** | חוסם התקדמות, או מסכן נתונים / כסף / בידוד |
| **HIGH**     | יגרום לנזק אמיתי אם יגיע לייצור כך         |
| **MEDIUM**   | מפריע לעבודה או לאיכות, לא מסוכן           |
| **LOW**      | סדר וניקיון                                |

---

## G-001 · תשתית Supabase · ~~CRITICAL~~ ~~MEDIUM~~ · **נסגר** ✅

**אומת 6.9.2026 — נסגר.** מה שהרשומה ביקשה היה אימות, לא הקמה. 59 מיגרציות
רשומות ב-`supabase_migrations` כמוחלות, והסכימה נבדקה טבלה־טבלה מול קבצי
המיגרציה: 169 טבלאות, RLS מופעל בכולן, `anon` בלי הרשאות טבלה, אף פונקציית
SECURITY DEFINER בלי `search_path` נעוץ. הראיה הנסיבתית הפכה להוכחה.

**תחום:** תשתית
**מה השתנה:** נסגר ברובו תוך כדי כתיבת המסמכים. כעת קיימים:
`@supabase/supabase-js` ו-`@supabase/ssr` בתלויות · `src/lib/supabase/`
(לקוח דפדפן, שרת, service-role, proxy, תרגום שגיאות) · `src/lib/env.ts`
עם אימות משתנים · `.env.example` · `.env.local` עם ערכים מוגדרים —
כלומר **פרויקט Supabase כנראה קיים**.

**מה עדיין פתוח:** לא אומת בסשן הזה שהמיגרציות **הורצו** מול המסד החי,
ולא שהאפליקציה מתחברת אליו בפועל. `.env.local` מוגדר — זו ראיה נסיבתית,
לא הוכחה.

**השפעה טכנית:** משימה 3 ב-M0 אינה חוסמת עוד. מה שנשאר הוא אימות,
לא הקמה.

**תיקון מומלץ:** `supabase db push` (או `psql` לפי `supabase/README.md`),
ואז להריץ את `supabase/tests/isolation.sql` ולפרסם את התוצאה — ראה `G-007`.

---

## G-002 · אף אפיון מלבד 3.0 אינו קיים במאגר כטקסט מקורי · **HIGH** · צומצם

**תחום:** תיעוד ומקורות אמת
**מה השתנה:** מאז כתיבת הרשומה נחתו שני מסמכי אפיון במאגר, והגיע אפיון
רביעי (5.0). **הפער כבר אינו "אין קבצים" אלא "הקבצים אינם המקור".**

| האפיון                | במאגר                                    | מה הקובץ                                                      |
| --------------------- | ---------------------------------------- | ------------------------------------------------------------- |
| **עומק 3.0**          | ✅ `docs/ESTIA_אפיון_עומק.txt` / `.docx` | **המקור עצמו**                                                |
| **4.0 · שכבת ה-SaaS** | ❌ **אינו במאגר בשום צורה**              | שחזור בלבד, מפוזר ב-`ARCHITECTURE.md`, `PLAN.md` ובקוד        |
| **AI Website Studio** | ✅ `docs/SPEC_WEBSITE_STUDIO.md`         | **קליטה מאורגנת** מהטקסט שנמסר — קרוב למקור, אך לא הטקסט עצמו |
| **5.0 · שבע החוויות** | ✅ `docs/SPEC_5.0.md`                    | ⚠️ **שחזור מתקציר.** מספרי הסעיפים לא נשמרו                   |
| **5.0 · רשת הסוכנים** | ✅ `docs/SPEC_AGENT_NETWORK.md`          | ⚠️ **שחזור מתקציר**                                           |

**מה עדיין פתוח:** אפיון 4.0 אינו במאגר כלל. שלושת המסמכים שכן נמצאים
**אינם הטקסט שנמסר** — שניים מהם שוחזרו מתקציר, ומה שהם חסרים מתועד בהם
במפורש (`SPEC_5.0.md` סעיף 11, `SPEC_AGENT_NETWORK.md` סעיף 15).

**השפעה על המשתמש:** אין ישירה. עקיפה: יכולת שהופיעה באפיון ולא הגיעה
לשחזור פשוט לא תיבנה, ואף אחד לא ידע.

**השפעה טכנית:** סשן עתידי יכול לאמת החלטה מול מסמך — **אך לא מול המקור.**
מה שמסומן 🔒 במסמכים נמסר במפורש ואפשר לבנות לפיו; כל דבר אחר טעון אימות
מול בעל המוצר. **המסוכן ביותר נשאר 4.0**, שהוא היחיד ללא מסמך כלשהו.

**תלויות:** בעל המוצר צריך לשלוח את הטקסט של 4.0, ואם שמורים אצלו — גם את
המקור של Website Studio, של 5.0 ושל רשת הסוכנים.

**תיקון מומלץ:** לשמור כ-`docs/ESTIA_אפיון_4.0.*`,
`docs/ESTIA_אפיון_5.0.*` ו-`docs/ESTIA_רשת_הסוכנים.*`, ואז להצליב מול
המסמכים המאורגנים ו-`CANONICAL_SPEC.md` ולתקן פערים. **הצלבת 5.0 דחופה
מהאחרות** — הוא היחיד שנכתב מתקציר ולא מטקסט שנמסר.

---

## G-003 · אין CI · **MEDIUM** · ~~HIGH~~ · פתוח

**אומת 6.9.2026 — נשאר פתוח, יורד ל-MEDIUM.** שלושה workflows קיימים
ומקומיטים: `ci.yml` · `database.yml` · `security.yml`. **לא ניתן היה לאמת
מכאן שאחד מהם רץ אי פעם** — אין `gh` מותקן והמאגר פרטי. הרשומה הסוגרת של
המסמך הזה קובעת שפער נסגר כשראו אותו עובד, ולא כשכתבו את הקוד שלו, ולכן
הוא נשאר פתוח. מה שכן ידוע: השער שה-workflow מריץ, `npm run gate`, עובר
מקומית בשלמותו.

**תחום:** תשתית ואיכות
**תיאור:** קיים `.github/scripts/check-secrets.mjs` וקיים שער מאוחד
`npm run verify` (format → lint → typecheck → test → security → build).
**אבל אין workflow שמריץ אותם.** `.github/workflows/` אינה קיימת,
ולכן שום שער אינו רץ אוטומטית.

**השפעה על המשתמש:** באג שנתפס בבדיקה יכול להגיע לייצור אם מישהו שכח
להריץ אותה מקומית.

**השפעה טכנית:** עם מהנדסים מקבילים, שבירה של אחד מתגלה אצל אחר במקום
ב-push. **חוק "אל תשאיר את הענף שבור ביודעין" נשען על זיכרון אנושי.**

**תלויות:** דורש שהמאגר יהיה על GitHub — ראה `G-013`. `G-011` חייב
להיסגר קודם, אחרת ה-CI יהיה אדום מהיום הראשון.

**תיקון מומלץ:** GitHub Actions על push ועל PR שמריץ `npm ci && npm run verify`.
השער כבר מוגדר — חסר רק מי שיריץ אותו.

---

## G-004 · אין E2E · **MEDIUM** · ~~HIGH~~ · צומצם

**אומת 6.9.2026 — צומצם, לא נסגר.** `e2e/` קיימת: `sweep.mjs` הולכת על כל
מסלול תחת `src/app/(app)` בתור כל אחת משמונה הפרסונות ומסווגת את התשובה,
לצד `crawl-links` · `dead-links` · `privacy`. זה שער אמיתי נגד מסך שנשבר
או נחשף לפרסונה הלא נכונה.

**מה שעדיין אין:** מסע משתמש מקצה לקצה. אף בדיקה לא יוצרת הזמנה, גובה
תשלום ומוציאה אורח. הפער הוא כבר לא "אין E2E" אלא "יש סריקה, אין מסע".

**תחום:** בדיקות
**תיאור:** אין תשתית בדיקות דפדפן ולא נבחר כלי.

**השפעה על המשתמש:** מסלול קריטי שנשבר — הזמנה, חתימה, תשלום — יתגלה
ע"י לקוח.

**השפעה טכנית:** [`TEST_STRATEGY.md`](TEST_STRATEGY.md) מגדיר קומה
שלישית שלא קיימת. חלק מהחוקים החשובים — "הזזה לתאריך תפוס נדחית בשרת",
"קישור מזויף לא מחזיר כלום" — נבדקים באמת רק מקצה לקצה.

**תלויות:** דורש ממשק. אין דחיפות לפני M1.

**תיקון מומלץ:** Playwright, עם המסלולים: onboarding · יצירת הזמנה ·
Guest Portal · חתימה · הזמנה ישירה מהאתר.

---

## G-005 · מערכת מצבי טעינה / ריק / שגיאה — חלקית · **LOW** · ~~HIGH~~ ~~MEDIUM~~ · צומצם

**אומת 6.9.2026 — צומצם ל-LOW, עם שארית מדודה.** 44 קבצי `loading.tsx`
פרוסים על המסכים. **גבולות שגיאה הם הצד הדק:** `src/app/error.tsx` אחד
לכל האפליקציה, ושני `not-found`. כלומר שגיאה במסך אחד מפילה את המסך
השורשי במקום להיעצר בגבול המודול. זו שארית אמיתית ולא סגירה.

**תחום:** UX ומערכת עיצוב
**מה השתנה:** נחתו `src/components/states/empty-presets.ts` ו-
`error-copy.ts` (נבדק, 17 בדיקות), וכן `button`/`card`/`badge`/`cn`/
`confirm-gate`. **הנוסח והדפוס קיימים.**

**מה עדיין פתוח:** אין רכיב טעינה, ואין כלל אכיפה שכל מסך מצהיר על
ארבעת המצבים — אין עדיין מסכים לבדוק אותו עליהם.

**השפעה על המשתמש:** בלי זה מגיעים למסך לבן בטעינה, "אין נתונים" בלי
הסבר מה לעשות, ושגיאה טכנית באנגלית מול בעל צימר. **הרושם הראשון של
מוצר נקבע בדיוק במצבים האלה.**

**השפעה טכנית:** הדפוס נקבע בזמן — לפני שנבנה מסך שיפתור לבד.

**תיקון מומלץ:** להשלים רכיב טעינה, ולאכוף את הכלל על המסך הראשון:
כל מסך מצהיר על ארבעת המצבים, ומצב ריק מסביר מה לעשות ומציע פעולה.

---

## G-006 · חולשת קישור האורח במערכת החיה · **HIGH** · מטופל בעיצוב

**תחום:** אבטחה
**תיאור:** נבדק ב-`_reference/estia_live.html`. הקישור האישי לאורח הוא
`?guest=<booking id>`, וה-`id` נוצר כ-`'b' + Date.now()` — חותמת זמן
במילישניות.

מכאן: ניתן לניחוש בטווח צר · אין תפוגה · אין ביטול · **הסוד הוא גם
המזהה הפנימי**, ולכן אי אפשר להחליף אחד בלי לשבור את השני.

**השפעה על המשתמש:** במערכת החיה — חשיפה תיאורטית של פרטי הזמנה של
אורח לצד שלישי. **המערכת החיה אינה נוגעים בה ואינה בטיפול.**

**השפעה טכנית:** דרישת עיצוב מחייבת ל-M2: אסימון אקראי קריפטוגרפית,
נפרד מהמזהה, עם תפוגה, ניתן לביטול, מוגבל להזמנה אחת, עם rate limiting.

**תלויות:** M2 · משימה 24.

**תיקון מומלץ:** לממש כמתואר, ולוודא שהבדיקה "לינק מזויף לא מחזיר
כלום; לינק ישן פג" קיימת. הפירוט: [`SECURITY_MODEL.md`](SECURITY_MODEL.md) סעיף 7.

---

## G-007 · הבידוד כתוב אך לא הוכח בהרצה · ~~CRITICAL~~ · **נסגר**

> **נסגר ב-27.08.2026, אחרי שהערך למטה נכתב.** הפער היה מנוסח נכון —
> הוא פשוט נסגר תוך כדי, על ידי מהנדס מקביל שסוכן התיעוד לא ראה.
>
> חמש המיגרציות הורצו על פרויקט Supabase חי (`shcauvnepnaemmnvahgy`)
> ועברו בניסיון הראשון. `supabase/tests/isolation.sql` הורץ:
> **52 טענות, 0 כשלים**, בטרנזקציה שהסתיימה ב-ROLLBACK.
>
> **ובדיוק כפי שהערך הזה חשש — נמצא ליקוי אמיתי שנראה תקין בקריאה.**
> `0004_rls.sql` הצהיר ש-`anon` לא מקבל דבר, אך שלוש הפונקציות
> המיוחסות היו זמינות לו דרך `/rest/v1/rpc/`, כי
> `REVOKE … FROM PUBLIC` אינו מבטל הענקה ש-Supabase נותנת ל-`anon`
> **בשמו המפורש**. תוקן ל-`REVOKE … FROM public, anon`, הסכימה הוקמה
> מחדש, ואומת עצמאית: 0 פונקציות נגישות ל-`anon`.
>
> **הלקח שנשמר בקוד:** הבדיקה כוללת **בקרות חיוביות** — אותן פקודות
> מכוונות לארגון של המבצע, שחייבות להשפיע על שורה אחת. בלעדיהן
> "0 שורות" מעיד גם על בידוד תקין וגם על מערכת שבורה לחלוטין, ו-52
> הטענות היו עוברות לשווא.
>
> **נותר לא-מוכח:** דבר לא נבדק דרך HTTP, ו-`properties`/`units`/`teams`
> יגיעו במיגרציה עתידית ויצטרכו מדיניות וטענות משלהן.

### הערך המקורי, כפי שנכתב

**תחום:** אבטחה
**מה השתנה:** רצפת האבטחה הראשונה **נכתבה** תוך כדי הסשן.
`supabase/migrations/0004_rls.sql` (586 שורות) מפעיל RLS על כל הטבלאות,
עם `my_organizations()`, מדיניות נפרדת לכל פקודה, ו-`WITH CHECK` על
כל `insert`. `supabase/tests/isolation.sql` (755 שורות) מוכיח אותה
כ-session אמיתי של `authenticated`, בתוך טרנזקציה שמסתיימת ב-ROLLBACK.

**מה עדיין פתוח — וזה העיקר:** **אף אחד בסשן הזה לא ראה את הבדיקה
הזאת רצה.** היא לא הורצה מול מסד.

**השפעה על המשתמש:** אין עדיין נתונים. ברגע שיהיו — זה הסיכון הקיומי.

**השפעה טכנית:** קוד RLS שנראה נכון וקוד RLS שהוכח נכון הם שני דברים
שונים. מדיניות אחת עם תנאי הפוך, טבלה אחת שנשכחה, פונקציה שרצה
כ-`security definer` בטעות — כולם נראים תקינים בקריאה. **זה הפער
שהכי קל לטעות לגביו**, כי הכל נראה ירוק.

**תלויות:** דורש חיבור למסד — `G-001`.

**תיקון מומלץ:**
`psql "$DATABASE_URL" -v ON_ERROR_STOP=1 -f supabase/tests/isolation.sql`,
ולתעד את התוצאה ב-[`PROJECT_STATE.md`](PROJECT_STATE.md) עם מספר.
**לפני** שנכתב פיצ'ר אחד מעל. ואז — בדיקת בידוד לכל טבלה חדשה,
ללא יוצא מן הכלל.

---

## G-008 · `.env.example` ואימות קונפיגורציה · **נסגר** ✅

**תחום:** תשתית
**מה נסגר:** נחת `.env.example` עם שני המשתנים והסבר למה המפתח הפומבי
בטוח בדפדפן ולמה מפתח ה-service-role אינו. `src/lib/env.ts` מאמת
בעלייה וזורק שגיאה שאומרת **איזה** משתנה חסר, ומונע קריאת מפתח
service-role בדפדפן. `.github/scripts/check-secrets.mjs` נוסף כשער.

**נותר לבדוק:** ש-`npm run security:check` באמת רץ — הוא חלק
מ-`npm run verify` שטרם הורץ בסשן הזה, ואין CI שיריץ אותו (`G-003`).

---

## G-009 · שלד עברית ו-RTL · **MEDIUM** · ~~פתוח~~ · צומצם

**תחום:** UI
**מה השתנה:** `src/app/layout.tsx` נכתב מחדש תוך כדי הסשן — פונטים
עבריים (Heebo לגוף, Frank Ruhl Libre לכותרות, עם `subsets: ["hebrew"]`),
מטא-דאטה בעברית, ותבנית כותרת `%s · ESTIA`.

**מה עדיין פתוח:** לא אומת בסשן הזה ש-`<html>` נושא `lang="he"`
ו-`dir="rtl"` — נבדק רק החלק העליון של הקובץ. `page.tsx` טרם נבדק מחדש.

**השפעה טכנית:** RTL שנוסף מאוחר דורש מעבר על כל מרווח, אייקון וכיוון
אנימציה. זול עכשיו, יקר אחר כך.

**תיקון מומלץ:** לאמת `lang="he" dir="rtl"` ולהחליף את `page.tsx`
אם הוא עדיין ברירת המחדל.

---

## G-010 · תמחור לא אומת מול לקוחות אמיתיים · **HIGH** · ממתין להכרעה

**תחום:** מוצר ועסקים
**תיאור:** ₪149 / ₪299 / ₪649 / ₪1,490 **נגזרו מהיגיון ערך, לא ממחקר
שוק**. גם הסף 2→4 יחידות ב-Basic, וגם "הקמה בליווי ₪490", מסומנים
ב-`docs/PACKAGES.md` סעיף 7 כטעונים אימות.

**השפעה על המשתמש:** מחיר גבוה מדי = אין לקוחות. נמוך מדי = הכנסה
שלא מכסה עלויות, **ו"לקוחות מוקדמים נועלים מחיר לתמיד" הופך את הטעות
לקבועה.**

**השפעה טכנית:** נמוכה — המחירים נתונים ניתנים לעריכה, וזו בדיוק הסיבה
שהמודל בנוי כך. אבל snapshot המחיר במנוי הופך כל לקוח מוקדם לבלתי הפיך.

**תלויות:** שיחות עם חמישה בעלי צימרים.

**תיקון מומלץ:** **לא לשאול "כמה היית משלם"** — כולם עונים על זה לא
נכון. לשאול: _"כמה הזמנות בחודש מגיעות אליך דרך Airbnb, וכמה זה עולה
לך בעמלות?"_ התשובה נותנת גם את המחיר וגם את משפט המכירה.

---

## G-011 · `npm run typecheck` נכשל בעץ נקי · ~~MEDIUM~~ · **נסגר** ✅

**אומת 6.9.2026 — נסגר.** הרשומה עצמה ביקשה להריץ שוב לפני שפועלים לפיה.
הורץ: `npm run typecheck` נקי בעץ נקי. יתר על כן הוא כבר אינו שער בודד —
הוא השלב השני מתוך שמונה ב-`npm run gate`, והחשש שברשומה ("CI יהיה אדום
מהריצה הראשונה ומישהו יתפתה להוריד את השער") לא התממש.

**תחום:** תשתית ואיכות
**תיאור:** בבדיקה ב-20:13, `npx tsc --noEmit` החזיר:

```
src/app/layout.tsx(20,50): error TS2304: Cannot find name 'LayoutProps'.
```

`LayoutProps` הוא טיפוס גלובלי ש-Next.js מייצר לתוך `.next/types` בזמן
`next dev` / `next build`. בלי תיקיית `.next`, `tsc` לבדו לא מוצא אותו.

**⚠️ `layout.tsx` נכתב מחדש מאז.** ייתכן שהפער נסגר אגב אורחא.
**יש להריץ `npm run typecheck` שוב לפני שפועלים לפי הרשומה הזאת.**

**השפעה על המשתמש:** אין.
**השפעה טכנית:** **`npm run typecheck` אינו שער עצמאי כרגע.** ברגע
ש-CI יוקם הוא יהיה אדום מהריצה הראשונה, ומישהו יתפתה להוריד את השער.

**תלויות:** קשור ל-`G-003` — חייב להיסגר לפניו.

**תיקון מומלץ:** להריץ `next build` לפני `tsc` ב-CI, או להימנע מהטיפוס
הגלובלי ולהצהיר על ה-props מפורשות ב-`layout.tsx` — מה שממילא יקרה
כשהקובץ יתוקן ב-`G-009`.

---

## G-012 · תצורת Vitest · **נסגר** ✅ · עם שארית LOW

**תחום:** בדיקות
**מה נסגר:** `vitest.config.ts` נחת — סביבת node, alias `@/`, timeout,
וספי כיסוי **שנמדדו ולא נוחשו** (statements 78 / branches 69 /
functions 82 / lines 78), עם הערה מפורשת למה הם דווקא שם.

**שארית (LOW):** הקובץ נטען כ-CommonJS ומייצר אזהרת Vite —
_"ESM syntax in a file loaded as CommonJS"_. הפתרון: `.mts` או
`"type": "module"`. לא מזיק היום, ייהפך לשגיאה בגרסת Vite עתידית.

---

## G-013 · הקוד אינו ב-git · ~~HIGH~~ · **נסגר**

> **נסגר ב-27.08.2026, אחרי שהערך למטה נכתב.**
> commit `11653d0` מכיל את הליבה — 46 קבצים — אחרי שכל ארבעת השערים
> עברו (typecheck, lint, 365 בדיקות, build). המאגר נדחף ל-GitHub
> **פרטי**: `github.com/guccigang1020/estia`.
>
> נבדק לפני הדחיפה ואומת אחריה: הקובץ היחיד מסוג `.env` שהגיע לענן
> הוא `.env.example` עם ערכי דמה. `.env.local` נשאר מקומי.
>
> נותר פתוח: Vercel נדחה להמשך לבקשת בעל המוצר, ולכן אין עדיין כתובת
> חיה. `G-003` (CI) כבר לא חסום.

### הערך המקורי, כפי שנכתב

**תחום:** תשתית
**תיאור:** כל `src/`, וכן `package.json`, `tsconfig.json`, `CLAUDE.md`,
`AGENTS.md` וקבצי התצורה — **כולם untracked**. ה-commit האחרון,
`c294f8b`, מכיל תיעוד בלבד.

**השפעה על המשתמש:** אין ישירה.
**השפעה טכנית:** עבודה של ימים יושבת בלי גיבוי ובלי היסטוריה. אין
נקודת חזרה, אין `git bisect`, ואין דרך לראות מי שינה מה. **חוסם את
`G-003`** — אין מה להריץ CI עליו. עם מהנדסים מקבילים זה גם מקור
לדריסות שקטות.

**תלויות:** M0 משימה 2 (GitHub פרטי + Vercel).

**תיקון מומלץ:** commit של המצב הנוכחי בהקדם, ואז GitHub פרטי.
**שים לב:** `.gitignore` מכיל `next-env.d.ts` — לוודא שזה מכוון.

---

## G-014 · אין תוכנית לגיבוי ולשחזור · **MEDIUM** · פתוח

**תחום:** תפעול
**תיאור:** `docs/PACKAGES.md` מבטיח "גיבויים" כחלק מרצפת המוצר בכל
חבילה. אין תוכנית: תדירות, שמירה, ו**בדיקת שחזור בפועל**.

**השפעה על המשתמש:** אובדן נתונים של עסק אירוח = אובדן ההזמנות שלו.
זה נזק שאין ממנו התאוששות מסחרית.

**השפעה טכנית:** גיבוי שלא נבדק בשחזור אינו גיבוי. Supabase מספק
גיבויים לפי מסלול — **וזה שיקול בהכרעת `G-001`**.

**תיקון מומלץ:** להגדיר תדירות ותקופת שמירה, ולתרגל שחזור אמיתי לפני
הלקוח המשלם הראשון.

---

## G-015 · אין תנאי שימוש ומדיניות פרטיות · **HIGH** · ממתין להכרעה

**תחום:** משפטי
**תיאור:** אין מסמכים משפטיים. המערכת תחזיק פרטי אורחים — שמות, טלפונים,
מספרי מסמך מזהה — ותפיק מסמכי מס.

**השפעה על המשתמש:** חשיפה רגולטורית לבעל המוצר. חוק הגנת הפרטיות
בישראל חל, וכך גם חובות עיבוד מידע עבור הלקוחות.

**השפעה טכנית:** עשוי לחייב יכולות מוצר: מחיקת נתוני אורח לפי בקשה,
ייצוא מידע אישי, הגדרת תקופות שמירה. **עדיף לדעת לפני שהסכימה ננעלת.**

**תלויות:** **עורך דין, לא מפתח.** מסומן ב-`docs/PLAN.md` סעיף 5
כנדרש לפני עלייה לאוויר.

---

## G-016 · לא נבחרו ספקי סליקה וחשבוניות · **MEDIUM** · ממתין להכרעה

**תחום:** אינטגרציות
**תיאור:** משימה 37 (סליקה) ומשימה 45 (חשבוניות) מסומנות ⛔.
המועמדים מאפיון 3.0: Grow · Tranzila · PayPlus · Stripe · מורנינג ·
iCount · SUMIT · PAYPER. ההמלצה: מורנינג או iCount, כי הם נותנים סליקה
וחשבונית ב-API אחד — כך שמודולים 8 ו-9 מתאחדים.

**השפעה על המשתמש:** חוסם את M4 ו-M5 — כלומר את היכולת לקבל כסף.
**השפעה טכנית:** לא חוסם עכשיו. יש להגדיר את שכבת התשלומים מאחורי
ממשק, כדי שהחלפת ספק לא תהיה שכתוב.

**תלויות:** נדרש לפני M4. השאלה המקדימה: **יש לבעל המוצר כבר מסוף
סליקה או חשבון מורנינג?**

---

## G-017 · טרם הוכרע מי הלקוח המשלם הראשון · **CRITICAL** · ממתין להכרעה

**תחום:** אסטרטגיית מוצר
**תיאור:** שני מסלולים: **א׳** — בעלי צימרים ווילות בודדים (רוב השוק,
החלטה מהירה, טענת מכירה חדה). **ב׳** — חברות ניהול נכסים (לקוח אחד =
עשרות נכסים ומנוי גבוה, וקונה בדיוק את מה ש-4.0 מתאר).

**ההמלצה בתוכנית:** מסלול א׳ תחילה, **על תשתית של מסלול ב׳**.
המודל המלא נבנה בכל מקרה ב-M0.

**השפעה על המשתמש:** קובע מה נבנה קודם — האתר (א׳) או מערכת הצוות (ב׳).
**השפעה טכנית:** M0 נדרש בשני המסלולים, ולכן **ניתן להתחיל בלי תשובה**.
**התשובה נחוצה לפני M3.**

**תלויות:** בעל המוצר. `docs/PLAN.md` מציין: _"אם כבר יש לך חברת ניהול
שממתינה — תגיד, וההמלצה מתהפכת."_

---

## G-018 · אין מדיניות observability ותצפית · **LOW** · פתוח

**תחום:** תפעול
**תיאור:** לא הוגדר לוגים מובנים, מעקב שגיאות, מזהה בקשה מקצה לקצה,
או התראות.

**השפעה טכנית:** `docs/ARCHITECTURE.md` דורש `request_id` בכל אירוע
Audit — כלומר מזהה מתאם קיים כדרישה **בלי מנגנון שמייצר אותו**.
בלי מעקב שגיאות, כשל בהפקת חשבונית (`G` בסיכון: מסמך חוקי) עלול
להיעלם בשקט.

**תיקון מומלץ:** להגדיר יחד עם שכבת השירות ב-M0 — מזהה בקשה, לוג מובנה,
ומעקב שגיאות. זול עכשיו, יקר להשתיל אחר כך.

---

## G-019 · Audit קיים כחוזה, בלי נתיב כתיבה ובלי בדיקות · ~~HIGH~~ · **נסגר** ✅

**אומת 6.9.2026 — נסגר.** `src/lib/audit/pipeline.ts` קיים עם
`pipeline.test.ts` לצדו, ורשומת ה-audit אינה אופציונלית: `defineOperation`
מריץ authorize → validate → rule → transaction → **audit** → event, והקורא
לא יכול לדלג על שלב. כותרת המסמך כבר סימנה את זה ב-20:52 באותו יום; שורת
המצב של הרשומה פשוט לא עודכנה, וזו בדיוק הסיבה לאימות הזה.

**תחום:** יכולת ביקורת
**תיאור:** `supabase/migrations/0005_audit.sql` יוצר את `audit_events`
כטבלת נספח בלבד — הרשאות `update`/`delete` נשללות **וטריגר זורק חריגה**,
שתי שכבות בנפרד. `src/lib/audit/events.ts` מגדיר את החוזה בצד TypeScript,
כולל `ActorType` שמבחין בין `user` · `system` · `ai_agent` · `platform_staff`.

**מה חסר:** **אף אחד לא כותב לטבלה.** אין שכבת שירות שקוראת לה, ו-
`vitest.config.ts` מציין במפורש ש-`src/lib/audit/events.ts` נמצא
**בכיסוי 0%** — הקובץ היחיד שמושך את הכיסוי הכולל למטה.

**השפעה על המשתמש:** בלי Audit, השאלה "מי שינה את המחיר" אין לה תשובה.
**זו לא בעיה שאפשר לתקן בדיעבד — אין דרך לתעד למפרע.**

**השפעה טכנית:** ככל שייבנו פעולות עסקיות בלי נתיב Audit, כך יגדל
מספר נתיבי הכתיבה שיצטרכו להיפתח מחדש.

**תיקון מומלץ:** לבנות את נתיב הכתיבה **יחד עם** שכבת השירות הראשונה,
לא אחריה. ולהוסיף בדיקות ל-`events.ts` — ואז להעלות את ספי הכיסוי
בהתאם, כפי שהקובץ עצמו מנחה.

---

## G-020 · מפתחות זרים זמנית פתוחים · ~~MEDIUM~~ · **נסגר** ✅

**אומת 6.9.2026 — נסגר.** שלושת המפתחות שהרשומה מנתה קיימים במסד:
`memberships.default_property_id` · `memberships.team_id` ·
`audit_events.property_id`. בסכימה 685 מפתחות זרים. מערכי ה-scope, שאין
להם FK מעצם היותם מערכים, שמורים בשני טריגרים — `tg_validate_scope_refs`
בכתיבה ו-`tg_guard_scope_references` במחיקה. החוב המודע נפרע.

**תחום:** מודל נתונים
**תיאור:** לפי `supabase/README.md`, `properties`, `units` ו-`teams`
יגיעו במיגרציה הבאה. עד אז `memberships.default_property_id`,
`memberships.team_id`, `audit_events.property_id` ומערכי ה-scope הם
`uuid` **בלי מפתח זר**.

**השפעה טכנית:** שלמות רפרנציאלית פתוחה — אפשר לכתוב מזהה שאינו קיים.
בשלב הזה אין נתונים אמיתיים, ולכן הסיכון נמוך. הוא **עולה** ברגע
שנוצרות חברויות אמיתיות לפני מיגרציה 0006.

**תיקון מומלץ:** מיגרציה 0006 — `properties` · `units` · `teams` —
ובאותה מיגרציה **לסגור את כל ההגבלות**. מתועד היטב בקוד, ולכן זה
חוב מודע ותחום, לא פער שנשכח.

---

## G-021 · ארבעה מודולים מהאפיון בלי שורת קוד · ~~HIGH~~ · **נסגר** ✅

**אומת 6.9.2026 — נסגר.** ארבעתם נבנו, אחד־אחד כפי ש-§164 דורש:

| מודול                     | מה נחת                                                                      |
| ------------------------- | --------------------------------------------------------------------------- |
| **Webhooks / Public API** | `0060`–`0061` · `src/lib/webhooks` · חתימה · שער SSRF · סוללת שליחה         |
| **Unified inbox**         | `0062`–`0063` · `src/lib/inbox` · שרשור · נקרא לכל אדם · 7 פעולות           |
| **Listing quality**       | `src/lib/listing-quality` · ציון נגזר בלי מיגרציה · 3 בדיקות `not_assessed` |
| **Revenue intelligence**  | `src/lib/revenue` · תפוסה · ADR · RevPAR · קצב · תמהיל ערוצים · `/revenue`  |

**החצי שלא נבנה, ונאמר במפורש במקום להמציא אותו:** `market data`.
`marketPosition` מדווח `not_in_product` בכל קלט, מאותה סיבה בדיוק ש-
`unit.market_position` מדווח `not_assessed` ב-listing-quality — אין מקור
מחירי מתחרים במוצר ולא מאחוריו, וכל מספר שהיה מופיע שם היה נקרא כנמדד.

### הערך המקורי, כפי שנכתב

**תחום:** מוצר
**תיאור:** ארבעה מודולים מ-MARKET LEADERSHIP COMPLETION PROGRAM אינם קיימים
כלל — לא `src/lib/<name>`, לא מסך, לא טבלה:

1. **Webhooks / Public API** — אין נתיב שדרכו מערכת חיצונית מקבלת אירוע או
   קוראת נתון. `FISCAL_WEBHOOK_EVENT_TYPES` כבר מוגדר ב-`fiscal/provider.ts`
   ומחכה לצד שיקבל.
2. **Listing quality** — אין ציון איכות לליסטינג ואין מה לשפר לפיו.
3. **Revenue intelligence / market data** — אין מקור נתוני שוק ואין תמחור
   שמסתמך עליו.
4. **Unified inbox** — `/inbox` קיים כמסך מעל טבלאות קנוניות, בלי מודול
   דומיין ובלי מושג של "הודעה" שאפשר להשיב עליה.

**השפעה טכנית:** אין. שום דבר קיים לא נשען עליהם, ולכן זה חוב מוצר ולא סיכון.
**תיקון מומלץ:** אחד־אחד. §164 באפיון מפורש שאין לבנות אותם במקביל.

---

## G-022 · אין bootstrap לצוות הפלטפורמה — הקונסולה כולה בלתי נגישה · **HIGH** · פתוח

**תחום:** תפעול ואבטחה
**תיאור:** `platform_staff` ריקה במסד החי. `authenticated` מחזיק בה **SELECT
בלבד**, ומדיניות ה-SELECT דורשת `is_platform_staff()` — כלומר רק מי שכבר
רשום שם יכול לראות אותה. INSERT שמור ל-`service_role` בלבד.

**השפעה טכנית:** ביצה ותרנגולת מכוונת, אבל היא לא נפתרה: **כל קונסולת
הפלטפורמה בלתי נגישה**, ואיתה `platform_set_autopilot_capability` — כלומר
**אי אפשר להעניק יכולת Autopilot לאף ארגון**, וזו כל נקודת המכירה של המודול
הזה. גם `platform_set_organization_status` ו-`platform_set_organization_capabilities`
מחוץ להישג יד.

**תיקון מומלץ:** נוהל bootstrap כתוב ומתועד — שורה אחת ב-`platform_staff`
שנוצרת עם מפתח service-role מחוץ למוצר, בידי מפעיל הפלטפורמה. זה תרחיש
הפעם-האחת היחיד שמצדיק את המפתח הזה, ולכן הוא צריך להיות מתועד במפורש
כדי שלא יהפוך להרגל.

---

## G-023 · שני משתמשים, אפס ארגונים — onboarding מעולם לא הושלם · **HIGH** · פתוח

**תחום:** מוצר ותפעול
**תיאור:** במסד החי: `auth.users` = 2 · `organizations` = 0 · `memberships` = 0.
מישהו נרשם פעמיים ואף פעם לא יצא מזה ארגון.

**השפעה טכנית:** כל טענה על המוצר החי היא כרגע תיאורטית. אין ארגון, ולכן
אין נכס, אין הזמנה, ואף מסך לא הראה נתון אמיתי מעולם. זה גם מה שחוסם את
G-024: אי אפשר לבדוק את מסלול ההצטרפות הראשונה בלי הצטרפות ראשונה.

**תיקון מומלץ:** להשלים הרשמה אחת מקצה לקצה דרך המסכים — לא לזרוע. אם
המסלול נשבר באמצע, זה הבאג הכי חשוב במאגר וכרגע אף אחד לא ראה אותו.

---

## G-024 · שבע טבלאות זהות ב-RLS enabled ולא forced · **MEDIUM** · חסום על G-023

**תחום:** אבטחה
**תיאור:** `memberships` · `membership_roles` · `roles` · `role_permissions` ·
`permissions` · `plans` · `platform_staff` — RLS מופעל ולא נכפה. הבעלים
(`postgres`) פטור מהמדיניות, וזה בכוונה: מסלול ההצטרפות הראשונה צריך לכתוב
חברות למי שעדיין אין לו אף חברות.

**השפעה טכנית:** נמוכה בפועל — הפונקציות בבעלות `postgres` שממילא
`rolbypassrls`. אבל 162 טבלאות אחרות כן נכפות, והשבע האלה הן בדיוק אלה
שההרשאות נשענות עליהן.

**תיקון מומלץ:** לכפות, ואז להוכיח שההצטרפות הראשונה עדיין עובדת. **לא
לפני G-023** — לכפות בלי ארגון חי לבדוק מולו זה לנעול את הדלת ולזרוק את
המפתח בלי לבדוק שיש חלון.

---

## G-025 · סוללת שחרור ההודעות בלי מתזמן · **MEDIUM** · פתוח

**תחום:** תפעול
**תיאור:** נתיב ה-sweep ששחרר הודעות דחויות קיים ומוגן ב-`SWEEP_RELEASE_SECRET`,
אבל אף מתזמן לא קורא לו. המשתנה וקורא ה-POST צריכים להיות מוגדרים בפריסה.

**השפעה טכנית:** עסק שמפעיל שעות שקטות מפסיק לשלוח תזכורות תשלום — בשקט.
זה בדיוק סוג התקלה שאף אחד לא מדווח עליה, כי שום דבר לא נשבר על המסך.

**תיקון מומלץ:** cron בפריסה + התראה כשהריצה לא קרתה, לא רק כשהיא נכשלה.

---

## G-026 · `DATABASE_URL` לא מוגדר — מיגרציות אינן אטומיות · **MEDIUM** · פתוח

**תחום:** תשתית
**תיאור:** בלי `DATABASE_URL` המיגרציות מוחלות משפט־אחר־משפט דרך API ולא
בטרנזקציה אחת.

**השפעה טכנית:** מיגרציה שנכשלת באמצע משאירה סכימה חצי-מוחלת, ואין
`ROLLBACK`. עד היום זה לא קרה, וזה מזל ולא תכנון.

**תיקון מומלץ:** `DATABASE_URL` לחיבור ישיר, והרצת מיגרציות ב-`psql` בתוך
טרנזקציה — כפי ש-`supabase/README.md` כבר מתאר.

---

## G-027 · מסכים בלי מודול דומיין · **LOW** · פתוח

**תחום:** ארכיטקטורה
**תיאור:** `/inbox` · `/integrations` · `/reports` · `/leads` · `/promotions` ·
`/quotes` שואלים טבלאות קנוניות ישירות מתוך `_lib/queries.ts` בלי
`src/lib/<name>` מאחוריהם.

**השפעה טכנית:** לקריאה בלבד זו בחירה לגיטימית ולא חוב — דוח מעל טבלאות
קנוניות לא צריך דומיין. הפער הוא שאין דרך מכנית להבדיל בין "דק בכוונה"
ל"טרם נבנה", ולכן `scripts/product-reality.mjs` לא יכול לשמור על ההבחנה.

**תיקון מומלץ:** להצהיר על הכוונה במקום שהבודק יכול לקרוא, כדי שמסך דק
בכוונה ייראה אחרת ממסך שנעצר באמצע.

---

## סיכום לפי חומרה

| חומרה        | פערים                                                         |
| ------------ | ------------------------------------------------------------- |
| **CRITICAL** | G-017                                                         |
| **HIGH**     | G-002 · G-006 · G-010 · G-015 · G-021 · G-022 · G-023         |
| **MEDIUM**   | G-003 · G-004 · G-009 · G-014 · G-016 · G-024 · G-025 · G-026 |
| **LOW**      | G-005 · G-018 · G-027 · (שארית G-012)                         |
| **נסגרו** ✅ | G-001 · G-007 · G-008 · G-011 · G-012 · G-013 · G-019 · G-020 |

`G-005` צומצם ל-LOW ולא נסגר, ולכן הוא יושב בשורת LOW ולא בשורת הנסגרים:
44 מסכי טעינה, אבל גבול שגיאה שורשי אחד בלבד.

> **מעודכן 6.9.2026.** ארבעה פערים נסגרו באימות הזה — `G-001` · `G-011` ·
> `G-019` · `G-020` — ו-`G-005` צומצם ל-LOW. ארבעתם נשאו שורת מצב שלא
> תיארה את המאגר: `G-019` נכתב "פתוח" בזמן ש-`pipeline.ts` כבר היה בעץ,
> `G-011` "לאימות מחדש" והאימות לא נעשה, ו-`G-020` "מתוכנן" כשכל שלושת
> המפתחות כבר במסד.
>
> `G-003` (CI) **נותר פתוח** מאותה סיבה בדיוק שנכתבה כאן ב-27.8: הקבצים
> קיימים, אבל לא נראתה ריצה. הפעם גם לא ניתן היה לבדוק — אין `gh` מותקן
> והמאגר פרטי. **פער נסגר כשראו אותו עובד.**
>
> `G-017` היא עדיין הקריטית היחידה, ועכשיו יש לה שכנה מעשית: `G-023`.
> אפשר להתווכח מי הלקוח המשלם הראשון כמה שרוצים, אבל במסד החי אין ולו
> ארגון אחד — ולכן אף מסך במוצר לא הציג נתון אמיתי מעולם.

## מה חוסם מה

```
G-001 (חיבור למסד)  ──►  G-007 (הרצת הוכחת הבידוד)  ──►  כל טענת בידוד

G-023 (אין ארגון חי)  ──►  G-024 (כפיית RLS)
                      ──►  כל טענה על המוצר החי

G-022 (אין צוות פלטפורמה)  ──►  קונסולת הפלטפורמה
                           ──►  הענקת יכולת Autopilot לכל ארגון

G-016 (אין ספק חשבוניות)  ──►  נתיב הכתיבה של fiscal

G-017 (לקוח ראשון)  ──►  סדר M3 מול M6
```

**שני פערים ניתנים לסגירה מיד, בלי אף הכרעה עסקית:** `G-022` (שורה אחת
ב-`platform_staff`, בידי מפעיל הפלטפורמה) ו-`G-026` (`DATABASE_URL`).

**הפעולה היחידה הכי בעלת ערך כרגע:** להשלים הרשמה אחת מקצה לקצה דרך
המסכים ולראות ארגון נוצר — `G-023`. היא סוגרת את הפער הכי גדול בין מה
שהמאגר טוען לבין מה שמישהו ראה קורה, ומשחררת את `G-024` אחריה.

---

## G-028 · `review.view` ו-`review.manage` בקטלוג בלי טבלה · **MEDIUM** · פתוח

**תחום:** מוצר והרשאות
**תיאור:** שתי ההרשאות קיימות בקטלוג ומוענקות בתפקידים. **אין טבלת ביקורות
באף מיגרציה.** ההענקות מצביעות על כלום.

נמצא תוך כדי בניית listing-quality, ולכן `property.guest_rating` מדווח
`not_assessed` ומסביר בעברית שאין מה למדוד — ולא שהנכס לא מדורג. אותה צורה
בדיוק ש-`message.assign` הייתה בה לפני `0063`.

**השפעה על המשתמש:** אורח לא יכול להשאיר ביקורת, ובעל עסק שרואה את ההרשאה
במסך התפקידים מסיק שהיכולת קיימת ורק כבויה.

**תיקון מומלץ:** או טבלה ומודול, או להסיר את שתי ההרשאות מהקטלוג. **הגרוע
משניהם הוא להשאיר כך** — הרשאה שאין מאחוריה דבר היא הבטחה שהמוצר לא עומד בה.

---

## G-029 · `try_cast_inet` בלי `search_path` נעוץ · **LOW** · פתוח

**תחום:** אבטחה
**תיאור:** יועץ האבטחה של Supabase מדווח עליה, והיא הפונקציה **היחידה**
ב-`public` בלי `search_path` נעוץ מתוך עשרות. **אינה `SECURITY DEFINER`**
(`prosecdef = false`), ולכן היא רצה בהרשאות הקורא והסיכון נמוך.

**תיקון מומלץ:** `alter function public.try_cast_inet(text) set search_path = ''`.
שורה אחת, ואז דוח היועץ נקי מהסוג הזה לגמרי — מה ששווה בפני עצמו, כי דוח עם
ממצא קבוע אחד הוא דוח שמפסיקים לקרוא.

---

## G-030 · הגנת סיסמאות שדלפו כבויה · **LOW** · פתוח

**תחום:** אבטחה
**תיאור:** יועץ Supabase: `Leaked Password Protection Disabled`. המערכת מקבלת
סיסמה שמופיעה בדליפות ידועות.

**השפעה על המשתמש:** בעל צימר שבוחר סיסמה שכבר דלפה במקום אחר, והחשבון שלו
מחזיק את פרטי כל האורחים שלו.

**תיקון מומלץ:** מתג בהגדרות Auth של הפרויקט. לא קוד.

---

## G-031 · `0064_first_workspace.sql` נכתבה ולא הוחלה · **HIGH** · חוסם את G-023

**תחום:** תשתית
**תיאור:** המיגרציה שמאפשרת הרשמה בלי סוד קיימת בעץ העבודה ו**לא רצה מול
המסד**. הניסיון להחיל אותה דרך ה-MCP נחסם על ידי מסווג ההרשאות של מצב
אוטומטי, ולא על ידי שגיאה במיגרציה.

**השפעה טכנית:** עד שתוחל, `create_first_workspace` אינו קיים,
`createWorkspace` נופל לנתיב הישן, שני המשתנים עדיין חסרים — ולכן **G-023
נשאר בדיוק כפי שהיה.** הקוד ב-`signup.ts` מטפל בזה נכון: `PGRST202` הוא
הקוד היחיד שמפיל אותו לנתיב החלופי, וכל סירוב אחר נשאר סירוב.

**זכור את החוק של המרשם הזה:** מיגרציה שמעולם לא רצה אינה מיגרציה שנבדקה.
הרפטיציה בסוף הקובץ **מריצה** את המשמרים במקום להצהיר עליהם — אבל היא רצה
רק כשהמיגרציה רצה.

**תיקון מומלץ:** להחיל, ואז להשלים הרשמה אחת דרך המסכים ולראות ארגון נוצר.

---

## G-032 · אין פריסה, ולכן אין כתובת · **HIGH** · פתוח

**תחום:** תשתית ומוצר
**תיאור:** `NEXT_PUBLIC_SITE_URL` הוא `http://localhost:3000`. לא הייתה
קונפיגורציית פריסה כלל עד היום; `vercel.json` ו-`docs/DEPLOYMENT.md` נחתו
בסשן הזה, **ואף פריסה לא בוצעה**.

**השפעה על המשתמש:** אין כתובת לשלוח אליה לקוח. ומעבר לכך — כל לינק אורח,
כל אתר שנוצר וכל כתובת callback ל-webhook יישאו `localhost` בתוכם, כי המשתנה
נצרב ל-bundle בזמן build.

**תלוי בו:** `G-025` (מתזמן) — ה-cron מוגדר ב-`vercel.json` ואינו קיים עד
שיש פריסה שתריץ אותו.

---

## אימות 6.9.2026, 23:30 — המסד זז, ועם ראיות

**`0064` ו-`0065` הוחלו.** 65 מיגרציות רשומות. מה שנסגר, וכיצד הוכח:

### G-031 · נסגר ✅ — המיגרציה הוחלה

הרפטיציה שבתוכה **רצה** ולא הצהירה: היא ניסתה ליצור ארגון בלי session,
דרשה שהניסיון ייכשל ב-`insufficient_privilege`, ובדקה שלא נשאר ארגון.
המיגרציה לא הייתה יכולה להסתיים אילו אחד מהם לא התקיים.

**הוכחה נפרדת, כקורא `authenticated` אמיתי, בטרנזקציה שהתגלגלה לאחור:**

```
PROOF org=f1ee3f6b… replay_same=t memberships=1 roles=1 scopes=1 subs=1
      status=onboarding tz=Asia/Jerusalem currency=ILS
GUARDS typo_timezone_refused=t anon_refused=t orgs_now=0
```

`replay_same=t` הוא הלחיצה הכפולה: קריאה שנייה עם אותו slug החזירה את **אותו
מזהה** ולא `23505`. `typo_timezone_refused=t` הוא `Asia/Jerusalm` — עובר את
אילוץ הטבלה, נדחה על ידי הפונקציה. אחרי הכול: **0 ארגונים.** לא נזרע דבר.

**ומעל HTTP, מול PostgREST החי:**

| קריאה                      | תשובה                                              |
| -------------------------- | -------------------------------------------------- |
| `create_first_workspace`   | `401` · `42501 permission denied for function`     |
| `workspace_slug_available` | `401` · `42501 permission denied for function`     |
| פונקציה שאינה קיימת        | `404` · `PGRST202 … not found in the schema cache` |

שתי הראשונות בזיכרון הסכימה ו-`anon` נדחה — לקח `0004` עומד גם דרך הרשת.
השלישית מוכיחה ש-`PGRST202` הוא באמת מה שמסד מחזיר על פונקציה חסרה, כלומר
נתיב ה-fallback ב-`signup.ts` מכוון לאות הנכון.

### G-024 · נסגר ✅ — 175 מתוך 175 נכפות

`0065` כופה את שבע טבלאות הזהות. **מה שאיפשר את זה הוא `0064`**: הרשומה
דרשה "לכפות ואז להוכיח שההצטרפות הראשונה עדיין עובדת", ועד עכשיו לא היה מול
מה להוכיח. הרפטיציה של `0065` **יוצרת מרחב עבודה תחת ההגדרה החדשה**, סופרת
את שורת החברות, ומגלגלת לאחור — מיגרציה ששברה את ההצטרפות הראשונה לא הייתה
יכולה להסתיים.

מדוד אחרי: `rls_enabled=175 · rls_forced=175 · rls_off=0`.

### G-029 · נסגר ✅ — `unpinned_functions = 0`

`try_cast_inet` ננעץ. אין יותר אף פונקציה ב-`public` בלי `search_path` נעוץ.

### G-023 · עדיין פתוח, אבל נשאר בו צעד אחד

החסימה הוסרה: הפונקציה קיימת, נגישה, ומוכחת. מה שנשאר הוא **כניסה אחת
והשלמת טופס ההצטרפות דרך המסכים** בחשבון שכבר מאומת (`buildmatriix@gmail.com`,
מאומת ב-27.8, נכנס פעם אחת ומעולם לא יצא מזה ארגון — בדיוק כפי שהיה צפוי).
**יצירת חשבונות אינה פעולה שאני מבצע**, ולכן הצעד הזה שלך.

---

### G-028 · נסגר ✅ — יש טבלה מאחורי ההרשאות

`0066_guest_reviews.sql` הוחלה (66 מיגרציות · 176 טבלאות). `review.view`
ו-`review.manage` כבר לא מצביעות על כלום.

**החוק שקובע אם הכול שווה משהו:** עסק **אינו יכול לערוך את מילות האורח או את
הציון, ואינו יכול למחוק ביקורת בכלל.** שלושה מנגנונים נפרדים, כי אחד הוא
העדפה ושלושה הם תכנון — אין הרשאת `delete`/`truncate` לאף תפקיד · טריגר דוחה
כל `UPDATE` שנוגע בציונים, בטקסט או במקור · הסתרה דורשת נימוק שנשמר גם על
השורה וגם ברשומת ה-audit.

זה לא נימוס. `/listings` קורא את השורות האלה עבור `property.guest_rating`
ואומר לעסק שהליסטינג שלו טוב. **דירוג שאפשר לנקות בשקט הוא שיווק בבגדים של
נתונים** — גרוע יותר מה-`not_assessed` שהוא מחליף, כי מאמינים לו.

**ועוד חוק:** ביקורת חייבת להתייחס לשהייה שקרתה. טריגר דוחה ביקורת על הזמנה
שלא הגיעה ליציאת האורח, ולוקח את הנכס, היחידה ותאריך היציאה **מההזמנה** ולא
מהקורא — הדרך הזולה ביותר לנפח דירוג היא ליצור הזמנות שלא היו ולדרג אותן.

הרפטיציה **הרצה** את דלת האורח עם טוקן שאינו טוקן ודרשה סירוב, ואז ודאה שלא
נכתבה ביקורת.

**מה שלא נבנה, ובמפורש:** טופס האורח עצמו. `guest_portal_submit_review`
קיים כדלת שהוא יצטרך, אבל `src/app/g/**` שייך לעבודה של סוכן אחר במאגר הזה
ולא נגעתי בו. עד אז המקור הזמין הוא `entered_by_host` — וזה לא מציין מקום:
רוב הצימרים בשוק הזה מקבלים פסקה בוואטסאפ ואין להם איפה לשים אותה.

`property.guest_rating` ב-listing-quality הפך לבדיקה אמיתית: `not_assessed`
בלי ביקורות ובפחות משלוש, ומשקל 2 מעליהן. נוספה `property.reviews_hidden` —
הממוצע כבר מחריג מוסתרות, ובלי השורה הזאת עסק היה יכול להסתיר את דרכו לציון
טוב והדוח היה מסכים איתו.
