# ESTIA · יומן אינטגרציה

**תאריך:** 2026-08-28
**מבקר:** Integration Lead (קריאה בלבד)
**גרסת עץ:** `main` @ `f78b8ee` + עבודה לא-מחויבת נרחבת של ~15 מהנדסים במקביל
**מסד נתונים חי שנבדק:** Supabase `shcauvnepnaemmnvahgy`

> כל ממצא כאן נצפה ישירות. שערים שלא הרצתי אינם מדווחים; טענות של מהנדסים
> נבדקו מול המאגר ולא התקבלו כראיה.

---

## פסק דין

**העץ קוהרנטי בבסיסו, אך לא עקבי בשכבת החוזים.** היסודות חזקים באופן יוצא דופן:
בידוד דיירים אכוף במסד (RLS על כל 33 הטבלאות, 0 חריגות), אין מפתחות קשיחים,
אין הרשאה לפי `role ===`, וכל פונקציית `SECURITY DEFINER` מוקשחת עם
`search_path=""`. מודול `finance/` החדש מדגים משמעת חוזים מופתית.

**הבעיה החמורה ביותר היא F-01: קיימים שני טיפוסי `DomainEventName` מתחרים.**
`src/lib/service/events.ts:16` מגדיר טיפוס מתירני
(``type DomainEventName = `${string}.${string}` ``) המסתיר את האיחוד הקפוא של
`src/lib/contracts/events.ts:120`. התוצאה אינה תיאורטית: **15 שמות אירוע נפרדים
נפלטים בפועל ואינם קיימים בקטלוג הקפוא** — 12 ב-`booking/state-machine.ts`
ו-3 ב-`preparation/operations.ts`. אלה אירועים שאיש אינו יכול להירשם אליהם,
שניתוב `ALERT_EVENTS` אינו רואה, ושעברו את הטייפצ'ק בשלמות. זהו בדיוק כשל
העבודה המקבילה שהקפאת הקטלוג נועדה למנוע — והוא בלתי נראה עד שמנוי אחד
מפסיק לעבוד.

הניגוד חד ומלמד: מהנדס ה-finance ייבא את הטיפוס הקפוא, ובמקום שבו הקטלוג
חסר אירוע רשם `event: null` והשאיר הערה מתועדת לבעל החוזה
(`payment-state-machine.ts:594`). מהנדס ה-booking ייבא את הטיפוס המתירני
מ-`../service` והמציא 12 שמות. אותה משימה, שתי תוצאות — ההבדל היחיד הוא
מאיזה קובץ יובא הטיפוס.

---

## תוצאות השערים — כפי שהורצו בפועל

הורצו ב-2026-08-28 04:06–04:08. כל השערים הורצו מחדש לאחר שהעץ השתנה מהותית.

| שער | פקודה | תוצאה |
|---|---|---|
| Typecheck | `npx tsc --noEmit` | ✅ **עובר** — פלט ריק |
| Lint | `npm run lint` | ✅ **עובר** — פלט ריק |
| Format | `npx prettier --check .` | ❌ **נכשל** — 29 קבצים |
| Tests | `npm test` | ❌ **נכשל** — 4 נכשלים / 1718 עוברים (1722), 1 קובץ מתוך 46 |
| Security | `npm run security:check` | ✅ **עובר** — 268 קבצים נסרקו, 0 הפרות |

**Prettier — 29 הקבצים:** `docs/spec/10-booking-engine.md`, `docs/spec/20-pricing.md`,
`docs/spec/40-guest-crm.md`, `agents/{commission,discounts,identity,phone,types}.ts`,
`finance/{expenses,payment-state-machine,payments,provider,types}.ts`,
`preparation/{complexity,delta,index,inventory,inventory.test,no-hardcoded-numbers.test,operations,readiness,requirements.test,rules,rules.test,sleeping.test,snapshot,types,work-plan,work-plan.test}.ts`

**Tests — 4 הכשלים, כולם ב-`src/lib/preparation/work-plan.test.ts`:**
שורש אחד משותף. `completeSection` ב-`work-plan.ts:384` זורק
`plan_section_blocked` על תלות בסקציה לא-גמורה, **לפני** שהוא מגיע לבדיקות
הצילום וה-override שהמבחנים מכוונים אליהן. המבחנים מצפים `/outstanding/i`
ו-`/נימוק/` ומקבלים `"Section bathrooms depends on incomplete sections: cleaning"`.

**סיווג:** מהנדס באמצע כתיבה על מודול ידוע-שבור, בתיקון כעת. **לא לתקן.**
זיהיתי ולא נגעתי.

**הערה על יציבות:** בהרצה מוקדמת יותר נצפה כשל ב-`hebrew-calendar/shabbat.test.ts`
(`expected 1044 to be 1043`). בהרצה חוזרת הוא נעלם — המהנדס תיקן את הקבוע
באמצע ההרצה. **אינו ממצא.** זו הסיבה שכל שער כאן הורץ לפחות פעמיים.

---

## אימות עצמאי מול המסד החי

בוצע ב-SQL קריאה-בלבד מול `shcauvnepnaemmnvahgy`.

| בדיקה | מה שנצפה |
|---|---|
| מיגרציות שהוחלו | **10** — עד `payments` (0010). `0011_operations.sql` קיים בדיסק ו**אינו מוחל** |
| טבלאות ב-`public` | **33** |
| RLS מופעל | **33 מתוך 33** — 0 טבלאות ללא RLS |
| `organization_id` | קיים בכל טבלה עסקית. 5 החריגות (`organizations`, `permissions`, `plans`, `role_permissions`, `user_profiles`) גלובליות בדין |
| הרשאות מוזרעות | **105** (מתוכן 7 `field`) |
| הרשאות בקוד | **145** (132 `PERMISSIONS` + 13 `FIELD_PERMISSIONS`) |
| **פער** | **40 הרשאות בקוד ללא שורה במסד**, ועוד `guest.view_contact` במסד ולא בקוד |
| `guest.view_contact` | עדיין מוענק ל-**7 תפקידים** במסד החי |
| `booking_status` enum | **19 ערכים — תואם ל-`BOOKING_STATUSES` בערך ובסדר במדויק** ✅ |
| `booking_source` / `hold_reason` / `price_line_kind` | 8 / 4 / 9 — כולם תואמים במדויק ✅ |
| פונקציות `SECURITY DEFINER` | 15, **כולן** עם `search_path=""` ✅ |
| פונקציות ניתנות להרצה ע"י `anon` | 15 (ראה F-10) |

**נקודת ייחוס קריטית:** ב-`HEAD` המחויב, הקוד מכיל 105 מענקים והמסד 105 —
**התאמה מדויקת, אפס סחיפה.** מלוא הפער של 40 נובע מעבודה לא-מחויבת. זו אינה
עדיין רגרסיה מול הבסיס המחויב, אך היא חוב שנצבר ללא שער שיתפוס אותו.

---

## ממצאים

### F-01 · CRITICAL · שני טיפוסי `DomainEventName` מתחרים; 15 אירועים מחוץ לקטלוג

**קובץ:** `src/lib/service/events.ts:16` מול `src/lib/contracts/events.ts:120`

`service/events.ts:16` מגדיר ``export type DomainEventName = `${string}.${string}` ``
— טיפוס המקבל כל מחרוזת מנוקדת. `service/index.ts:32` מייצא אותו מחדש.
`contracts/events.ts:120` מגדיר את האיחוד הקפוא של 74 האירועים.

הצרכנים התפצלו:
- `src/lib/booking/state-machine.ts:72` → מייבא מ-`'../service'` (מתירני)
- `src/lib/finance/payment-state-machine.ts:45` → מייבא מ-`'../contracts/events'` (קפוא)
- `src/lib/finance/payments.ts:36` → מייבא מ-`'../contracts/events'` (קפוא)

**מה שנפלט בפועל מחוץ לקטלוג (נמדד, לא משוער):**

`booking/state-machine.ts` — 18 שמות, 6 בקטלוג, **12 מחוץ לו:**
`booking.quoted`, `booking.optioned`, `booking.payment_requested`,
`booking.deposit_paid`, `booking.contract_sent`, `booking.pre_arrival`,
`booking.ready_for_check_in`, `booking.in_house`, `booking.checkout_pending`,
`booking.inspection`, `booking.deposit_released`, `booking.review_requested`

`preparation/operations.ts` — **3 מחוץ לקטלוג:**
`preparation.plan.built`, `preparation.plan.changed`, `preparation.section.overridden`
(הקטלוג מכיל רק `preparation.calculated` ו-`preparation.changed` — `preparation.plan.changed`
הוא כמעט-פגיעה ב-`preparation.changed`, בדיוק סוג הסחיפה שההקפאה נועדה למנוע)

חלקם מתנגשים עם שמות קיימים בקטלוג בניסוח אחר: `booking.contract_sent`
מול `contract.sent` שבקטלוג, ו-`booking.deposit_released` מול `deposit.released`.
שני שמות לאותה עובדה עסקית — וכל מנוי יקשיב לאחד מהם בלבד.

**למה זה חשוב:** אוטומציות, התראות, לוח מחוונים ונתיב הביקורת מתבססים על
השמות האלה. אירוע מחוץ לקטלוג הוא אירוע שאיש אינו יכול להירשם אליו,
ו-`ALERT_EVENTS` לעולם לא ינתב אותו לאדם. הטיפוס המתירני הוא בדיוק מה
שאיפשר לכל 15 לעבור טייפצ'ק נקי.

**תיקון מומלץ:** למחוק את ההגדרה המקומית ב-`service/events.ts:16` ולייצא
מחדש את הטיפוס הקפוא מ-`contracts/events.ts`. הטייפצ'ק יאיר מיד את כל 15
המקומות. לאחר מכן להכריע לכל אחד: להוסיף לקטלוג (עם מיגרציה ומבחנים יחד,
כפי שהחוזה דורש), או למפות לשם קיים. לאמץ את הדפוס של `finance` — `null`
מתועד עדיף על שם מומצא.

---

### F-02 · HIGH · קטלוג ההרשאות והמסד חלוקים על מי רשאי לראות טלפון של אורח

**קבצים:** `src/lib/authz/permissions.ts` מול `supabase/migrations/0002_authz.sql:214`

הקוד: 132 `PERMISSIONS` + 13 `FIELD_PERMISSIONS` = **145**.
הזרע ב-`0002_authz.sql`: **105** שורות. המסד החי: **105**.

**40 מענקים בקוד ללא שורה במסד**, ובהם משפחות שלמות:
`agent.*` (7), `agent_agreement.*`, `agent_limits.manage`, `commission.*` (3),
`agent_statement.*`, `agency.manage`, `report.agent.view`, `availability.view`,
`hold.*` (4), `lead.*` (4), `quote.*` (4), `payment.request_link`,
`booking.amend_*` (4), `rate.view_*` (3), `guest.view_{name,phone,email}`,
`booking.view_payment_status`.

**החמור מביניהם:** `guest.view_contact` פוצל בקוד ל-`guest.view_name`,
`guest.view_phone` ו-`guest.view_email`. הזרע לא עודכן. במסד החי
`guest.view_contact` עדיין **מוענק ל-7 תפקידים**, ואף אחת משלוש ההרשאות
החדשות אינה קיימת שם.

**למה זה חשוב:** זו אינה בעיית תיעוד. ההיגיון המוצהר בפיצול הוא שסוכן חיצוני
יקבל טלפון כדי להתקשר ללקוח שלו ולעולם לא אימייל, שהוא ערוץ העסק לשהות
הבאה. אכיפה זו אינה קיימת במסד: אין שם `guest.view_phone` ואין
`guest.view_email`, ומי שהחזיק `guest.view_contact` מחזיק בשתיהן בפועל.
בנוסף, כל קוד שיבקש מענק מ-40 החסרים לא ימצא אותו.

**עדכון תוך כדי הביקורת — התיקון נכתב ואינו מוחל.**
`supabase/migrations/0012_permission_catalogue.sql` (1492 שורות) הופיע בדיסק
במהלך הביקורת. בדקתי אותו: הוא מזריע **בדיוק 145 מענקים** — התאמה מלאה
לקטלוג הקוד — ומטפל בפיצול כראוי ובסדר הנכון: מעניק לכל תפקיד שהחזיק
`guest.view_contact` את **שתי** ההרשאות המחליפות (שורות 276–277), מעניק
`guest.view_name` לכל מי שהחזיק `guest.view` (שורה 285), ורק אז מסיר את
`guest.view_contact` מ-`role_permissions` ומ-`permissions` (שורות 293, 303).

**אך המסד החי עדיין מראה 105 הרשאות ו-`guest.view_contact` מוענק ל-7 תפקידים.**
המיגרציה אינה מוחלת. הפער שתואר לעיל חי כרגע במלואו; מה שהשתנה הוא שהתיקון
קיים וממתין להחלה, ולא שהוא בוצע.

**מה שנותר:** להחיל את 0012, ולאמת אחריה שספירת הטבלה היא 145 ושאין שורה
עם `guest.view_contact`. בנוסף — **מבחן שער שמשווה את `PERMISSIONS` לטבלה**,
שאינו קיים; בלעדיו הפער ייפתח שוב בשבוע הבא, שכן שום שער נוכחי לא היה תופס
אותו כשנפתח.

---

### F-03 · HIGH · שני מבני `DomainEvent` שאינם תואמים; זה שבשימוש חסר `idempotencyKey`

**קובץ:** `src/lib/contracts/events.ts` מול `src/lib/service/events.ts:24`

| שדה | `contracts` (הקפוא) | `service` (בשימוש בפועל) |
|---|---|---|
| `occurredAt` | `string` | `Date` |
| `idempotencyKey` | קיים, חובה | **חסר** |
| `resourceType` / `resourceId` | קיימים | **חסרים** |
| `actorUserId` | קיים | **חסר** |

הצינור (`defineOperation`) משתמש בגרסת `service`. התיעוד בגרסה הקפואה קובע
במפורש ש-`idempotencyKey` הוא "יציב על פני ניסיונות חוזרים של אותו אירוע
לוגי, כדי שמטפל יוכל לסרב לפעול פעמיים" — והצינור שבשימוש אינו נושא אותו כלל.

**למה זה חשוב:** כלל האידמפוטנטיות במגילה חל במפורש על התראות ועבודות רקע.
בלי מפתח על גבי האירוע, מטפל שרץ פעמיים אינו יכול לדעת שראה את האירוע הזה.
`resourceType`/`resourceId` חסרים אומרים שמנוי חייב לחלץ את מזהה המשאב
מה-payload — בדיוק ההסקה שהחוזה נכתב כדי למנוע.

**תיקון מומלץ:** לאחד למבנה אחד. הגרסה הקפואה היא הנכונה; להעביר את הצינור
אליה, כולל `occurredAt` כמחרוזת ISO (כפי שהמגילה דורשת לתאריכים) והטבעת
`idempotencyKey` בשלב הפרסום.

---

### F-04 · HIGH · שני נרמולי טלפון עם סטייה נמדדת, ואף מבחן אינו מקשר ביניהם

**קבצים:** `src/lib/agents/phone.ts:130` (TS) מול
`public.normalize_phone_il` ב-`supabase/migrations/0009_booking_core.sql:256` (SQL)

הגרסה ב-SQL היא **מפתח הדדופליקציה** של אורחים: עמודה מחושבת
`phone_e164 generated always as (public.normalize_phone_il(phone)) stored`
המגבה אינדקס ייחודי. הגרסה ב-TS היא זהות הסוכן.

**הרצתי את שתיהן על אותם 16 קלטים. הן מסכימות על 8 וחלוקות על 8:**

| קלט | SQL | TS |
|---|---|---|
| `0501234567` | `+972501234567` | `+972501234567` ✅ |
| `+972 (0)50-1234567` | `+972501234567` | `+972501234567` ✅ |
| `031234567` (קו נייח) | `+97231234567` | `REJECT:not_mobile` ❌ |
| `02-6543210` | `+97226543210` | `REJECT:not_mobile` ❌ |
| `0721234567` | `+972721234567` | `REJECT:not_mobile` ❌ |
| `9721234567` | `+9721234567` | `REJECT:too_long` ❌ |
| `+14155552671` | `+14155552671` | `REJECT:not_israeli` ❌ |
| `05012345` (חסר ספרה) | `+9725012345` | `REJECT:too_short` ❌ |
| `050123456789` | `+97250123456789` | `REJECT:too_long` ❌ |
| `1234` | `+1234` | `REJECT:too_short` ❌ |

**למה זה חשוב:** אין כלל אחד סמכותי ל"מספר טלפון ישראלי". טבלת האורחים
תייצר מפתח דדופליקציה למספר פגום או לקו נייח שהמוצר במקום אחר מכנה לא-חוקי.
אדם שהוא גם אורח וגם סוכן עלול לקבל שני מפתחות שונים. אין שום מבחן המשווה
בין השתיים — חיפשתי ולא קיים.

**תיקון מומלץ:** ה-SQL חייב להישאר הסמכות (הוא מגבה אינדקס ייחודי ולא ניתן
לדלג עליו). להגדיר את גרסת ה-TS כ**תת-קבוצה מחמירה** מוצהרת, ולהוסיף מבחן
שקובע: לכל קלט שה-TS מקבל, הפלט זהה בדיוק לפלט ה-SQL. הסטייה בכיוון השני
(SQL מקבל, TS דוחה) לגיטימית ומכוונת — אך צריכה להיות מתועדת ככזו.

---

### F-05 · MEDIUM · `LINEN_STATES` הוא אוצר מילים מקביל ל-`INVENTORY_STATES` הקפוא

**קובץ:** `src/lib/preparation/types.ts:442`; מכונת המצבים ב-`inventory.ts:238`

| קפוא (`contracts/states.ts:143`) | מקומי (`preparation/types.ts:442`) |
|---|---|
| `available` · `reserved` · `in_use` · `dirty` · `laundry` · `damaged` · `out_of_service` | `available` · `assigned` · `in_use` · `dirty` · `laundry` · `clean` |

`reserved` בחוזה ו-`assigned` המקומי הם אותו מושג — התיעוד בחוזה מתאר את
`reserved` כ"מה שמונע משני אירועים להבטיח את אותם עשרים מזרנים", וזה בדיוק
מה ש-`assigned` עושה. המקומי מוסיף `clean` ומאבד את `damaged` ו-`out_of_service`,
כך שמצעים פגומים אינם ניתנים לייצוג כלל.

עדות פנימית לבלבול: `StockLevel` באותו קובץ מכיל שדה `reserved: number`
לצד `byState` הממופתח ב-`assigned` — שתי מילים לאותו מושג באותו מבנה.

**`INVENTORY_STATES` אינו מיובא באף מודול.** אימתתי.

**תיקון מומלץ:** להכריע אם מצעים הם מקרה פרטי של מלאי (אז לייבא את
`INVENTORY_STATES` ולהוסיף `clean` לחוזה במיגרציה משותפת) או תחום נפרד
(אז לתעד זאת מפורשות בחוזה, כפי ש-`states.ts` כבר עושה עבור סטטוסי הזמנה).
ההכרעה השנייה לגיטימית — השתיקה הנוכחית אינה.

---

### F-06 · MEDIUM · `roundAgorot` ממומש פעמיים

**קבצים:** `src/lib/booking/pricing.ts:328` ו-`src/lib/metrics/rounding.ts:45`

שניהם "עיגול חצי הרחק מאפס". השוויתי: זהים לכל ערך סופי **פרט ל-`-0`** —
גרסת `pricing` מנרמלת ל-`0` באמצעות `+ 0`, גרסת `metrics` מחזירה `-0`.
(`Object.is(-0, 0)` הוא `false`, ומבחן עם `toBe(0)` ייכשל על `-0`.)

עדות לכך שהן נחשבות כברות-החלפה: `finance/money.ts` מייבא `roundAgorot`
מ-`../booking/pricing` (שורה 46) ובאותה נשימה `allocateEvenly`
מ-`../metrics/rounding` (שורה 49).

**תיקון מומלץ:** פונקציה סמכותית אחת. כלל עיגול הכסף שייך ל-`contracts/`
או למודול כספים ייעודי — לא למודול התמחור ולא למודול המדדים — ושני
הקוראים הנוכחיים יהגרו אליה.

---

### F-07 · MEDIUM · טיפוס הכסף `Agorot` מוכרז פעמיים

**קבצים:** `src/lib/plans/plan.ts:25` ו-`src/lib/booking/types.ts:188`

שתי הכרזות זהות של `export type Agorot = number`. התיעוד ב-`booking/types.ts`
אף מפנה ל-`plans/plan.ts` "לנימוק" — כלומר הכפילות מוכרת ולא נפתרה.

התוצאה המבנית: **כל מודול ה-`finance` שואב את טיפוס הכסף שלו מ-`booking/types`**
(`money.ts:47`, `payments.ts:34`, `provider.ts:46`, `refunds.ts:35`,
`deposits.ts:33`) — מודול התשלומים תלוי במודול ההזמנות עבור המושג הבסיסי ביותר שלו.

השורש: `contracts/states.ts` — בית אוצרות המילים הקפואים — **אינו מגדיר כסף כלל**,
ולכן שני מודולים גלגלו לעצמם טיפוס.

**תיקון מומלץ:** להעביר את `Agorot` (ואת `CURRENCY`) ל-`contracts/`,
ולהפוך את שתי ההכרזות הקיימות לייבוא.

---

### F-08 · MEDIUM · שער ה-format אדום, וה-CI ייכשל עליו

**מקור:** `npx prettier --check .` — 29 קבצים · `.github/workflows/ci.yml:44`

ה-CI מריץ `npm run format:check` כשלב הראשון. בעץ הנוכחי הוא נכשל, כלומר
**הענף אדום מול השער של עצמו** — בניגוד לכלל "לעולם אל תשאיר את הענף שבור
ביודעין".

**תיקון מומלץ:** `npm run format` — אך **לא לפני** שהמהנדסים בעלי העריכות
הפתוחות סיימו, אחרת עיצוב-מחדש רוחבי יתנגש בעבודתם. זו פעולה לתיאום, לא
לביצוע מיידי.

---

### F-09 · MEDIUM · 4 מבחנים נכשלים ב-`work-plan` — ידוע ובתיקון

**קובץ:** `src/lib/preparation/work-plan.ts:384`, מבחנים ב-`work-plan.test.ts:215,250,279,283`

שורש אחד: `completeSection` זורק `plan_section_blocked` על תלות בסקציה
לא-גמורה לפני בדיקות הצילום וה-override. הסדר בין הבדיקות הוא ההכרעה החסרה.

**סיווג:** מהנדס באמצע כתיבה. לא נגעתי. מדווח לשלמות התמונה בלבד.

---

### F-10 · MEDIUM · מיגרציה 0009 לא הקשיחה את הרשאות ההרצה כפי שקודמותיה עשו

**מקור:** נצפה ישירות ב-`pg_proc.proacl`; אושר ע"י Supabase advisor

שלוש פונקציות מ-0009 נושאות `=X/postgres` ב-ACL, כלומר **PUBLIC יכול להריץ**:
`booking_status_occupies`, `occupying_booking_statuses`, `terminal_booking_statuses`.

לשם השוואה, המיגרציות המוקדמות הקשיחו כראוי — `has_permission`,
`my_organizations`, `property_in_scope`, `unit_in_scope` נושאות ACL מפורש
ל-`authenticated`/`service_role` בלבד. גם `normalize_phone_il` באותה מיגרציה
0009 הוקשחה נכון (שורות 317–318). ההקשחה פשוט לא הוחלה על שלוש הפונקציות הללו.

בנוסף, ה-advisor מסמן 5 פונקציות טריגר `SECURITY DEFINER` כניתנות להרצה
ע"י `anon`: `tg_bookings_freeze_total`, `tg_bookings_record_status`,
`tg_bookings_sync_occupancy`, `tg_holds_sync_occupancy`, `tg_price_lines_recalc_total`.

**הערכת סיכון כנה:** הניצולות נמוכה. שלוש הפונקציות הראשונות טהורות ומחזירות
רשימת סטטוסים שאינה סוד; פונקציות טריגר אינן ניתנות להפעלה משמעותית דרך RPC
כי הן דורשות הקשר טריגר. **אין כאן דליפת נתונים.** מה שכן קיים הוא סטייה
מדפוס הקשחה מבוסס — והדפוס הוא שחשוב: הפונקציה הבאה שתיווסף באותה מיגרציה
בהעתק-הדבק עלולה להיות כזו שכן נוגעת בנתונים.

**תיקון מומלץ:**
`revoke all on function ... from public, anon;` לשלוש הפונקציות ולחמשת
הטריגרים, בהתאמה לדפוס שכבר קיים ב-0009 שורה 317.

---

### F-11 · MEDIUM · שתי מיגרציות קיימות בדיסק ואינן מוחלות

המסד החי מראה 10 מיגרציות, האחרונה `payments` (0010). בדיסק קיימות
`0011_operations.sql` ו-`0012_permission_catalogue.sql` שטרם הוחלו.

0011 לבדה תהיה LOW. 0012 מעלה את החומרה, משום שהיא התיקון ל-F-02 — כל עוד
היא ממתינה, פער 40 ההרשאות ופיצול `guest.view_contact` חיים במסד. כל טענה
על טבלאות תפעול או על קטלוג ההרשאות הנשענת על המסד החי אינה ניתנת לאימות כרגע.

---

### F-12 · LOW · בתי NUL גולמיים בשני קבצי מקור

**קבצים:** `src/lib/metrics/facts.ts` (היסט 5526, שורה 129),
`src/lib/preparation/requirements.ts` (היסט 9986, שורה 318)

בכל אחד בית `0x00` יחיד, בתוך תבנית מחרוזת כמפריד מפתח מורכב:
``​`${unitId}\0${night}`​``. הכוונה תקינה — NUL הוא מפריד בטוח שאינו יכול
להופיע במזהה — אך הוא נכתב כבית גולמי ולא כבריחה ` `.

**התוצאה מעשית:** `grep -r` מסווג את שני הקבצים כבינאריים ומדלג עליהם.
כלומר כל חיפוש רוחבי עתידי — כולל ביקורת כזו — יפספס אותם בשקט. נתקלתי
בזה בעצמי במהלך הביקורת.

**תיקון מומלץ:** להחליף את הבית הגולמי ב-` `. ערך זהה בזמן ריצה,
והקובץ נשאר טקסט.

---

### F-13 · LOW · תיעוד שאבד סנכרון עם הסכימה

**קובץ:** `src/app/(app)/dashboard/page.tsx`

הערת התיעוד קובעת: "אין הזמנות, נכסים ותשלומים בסכימה עדיין, ולכן כל נתון
כאן יהיה מומצא". זה היה נכון ואינו נכון עוד — 0008, 0009 ו-0010 הוחלו והטבלאות
קיימות במסד החי. הנימוק להיעדר מספרים במסך עדיין תקף (אין נתונים), אך
ההצדקה הכתובה שגויה.

בנוסף: `project-memory/OPEN_GAPS.md:88` — `G-003 · אין CI · HIGH · פתוח`
אינו נכון עוד. `.github/workflows/ci.yml` קיים ומריץ format, lint, typecheck,
test, coverage ו-build.

---

### F-14 · LOW · `FORCE ROW LEVEL SECURITY` חסר על 6 טבלאות

`memberships`, `membership_roles`, `roles`, `role_permissions`, `permissions`, `plans`
— RLS מופעל אך לא `FORCE`, בעוד 27 הטבלאות האחרות כן.

`permissions` ו-`plans` הן קטלוגים גלובליים ואינן זקוקות לכך. ארבע האחרות
הן טבלאות ההרשאה עצמן. `FORCE` משפיע רק על בעל הטבלה (`postgres`);
`authenticated`/`anon` כפופים ל-RLS בכל מקרה, ולכן **הסיכון המעשי נמוך**.
זו חוסר-עקביות בהקשחה על הטבלאות הרגישות ביותר, לא חור.

---

## מה שנבדק ונמצא תקין

לא כל ביקורת חייבת למצוא. הדברים הבאים נבדקו ישירות ועברו:

- **בידוד דיירים** — RLS על 33/33 טבלאות; `organization_id` בכל טבלה עסקית;
  5 החריגות גלובליות בדין.
- **`search_path`** — כל 15 פונקציות ה-`SECURITY DEFINER` נושאות `search_path=""`.
- **סודות** — `SUPABASE_SERVICE_ROLE_KEY` נקרא אך ורק ב-`src/lib/env.ts:58-59`.
  אפס מפתחות או כתובות פרויקט קשיחים תחת `src/`. `security:check` נקי על 268 קבצים.
- **הרשאה** — אין ולו `role ===` אחד המכריע הרשאה. שתי המופעים
  (`roles.ts:681-682`) נמצאים בתוך `grantsForSystemRole` — פונקציית התרגום
  תפקיד→מענקים, שהיא המקום היחיד הלגיטימי.
- **`can.ts` החדש** — `requires_approval` נצרך עקבית ב-`agents/access.ts:485`,
  `agents/discounts.ts` ו-`errors/safe-response.ts:88`. כל 6 ערכי `DenialReason`
  מכוסים ב-`safe-response.ts`. `scopeFor` ו-`scopeOverrides` עקביים.
  התיעוד מסביר נכונה מדוע `authorize()` לעולם אינו מחזיר `requires_approval` בעצמו.
- **enum ההזמנות** — 19 ערכים במסד, תואמים ל-`BOOKING_STATUSES` **בערך ובסדר במדויק**.
  כך גם `booking_source`, `hold_reason`, `price_line_kind`.
- **שערי תוכנית** — כל 16 מענקי רשת הסוכנים החדשים ממופים נכון ל-`agent_network`,
  ו-`rate.view_agent`/`rate.view_net` ל-entitlements שלהם. מה שאינו ממופה
  הוא ליבה במכוון, כפי שהקובץ מצהיר.
- **ניווט** — `menu.ts` מסמן מסלולים שטרם נבנו כ-`planned` עם `href: null`.
  אין ולו קישור אחד למסך שאינו קיים.
- **מילון** — אפס מופעים של `company_id`/`tenant_id`/`business_id`. `organization` בלבד.
- **`invoice_sequences`** — RLS מאולץ עם אפס מדיניות. בדקתי את הכוונה:
  **מכוון ונכון** — הגישה עוברת רק דרך `next_invoice_number` שהיא
  `SECURITY DEFINER` ומורשית מ-`public, anon, authenticated`. כשל-סגור בתכנון.
- **משמעת חוזים ב-`finance`** — מופתית. מייבא את הקטלוג הקפוא, ובמקום שחסר בו
  אירוע רושם `event: null` ומשאיר הערה מתועדת לבעל החוזה במקום להמציא שם.
  זהו הדפוס שכל מודול אחר צריך לאמץ.
- **חלוקת אחריות במודול ההזמנות** — `holdOverlaps` מאציל ל-`rangesOverlap`;
  `availability.ts` מייבא את בדיקת החפיפה מהחוזה ואינו מגלגל משלו;
  `metrics/rows.ts` **גוזר** את `POST_STAY_STATUSES` מחוזה ההזמנות במקום לשכפלו.
  אין בדיקת חפיפה שנייה ואין חישוב תפוסה שני.

---

## מה שלא הצלחתי לאמת

נאמר במפורש, ללא ריכוך:

1. **`0011_operations.sql`** — אינו מוחל למסד. לא בדקתי את תוכנו מול סכימה חיה.
2. **`supabase/tests/*.sql`** (4 קבצים, כולל `isolation.sql`) — **אף שער אינו
   מריץ אותם.** `ci.yml` אינו מזכיר אותם ואין להם סקריפט ב-`package.json`.
   הוכחת בידוד הדיירים ש-`OPEN_GAPS.md` G-007 מסמן כ"נסגר" היא ארטיפקט
   ידני חד-פעמי ולא אינvarianta שמורה. לא הרצתי אותם (דורש `psql` ישיר).
3. **התנהגות בזמן ריצה של המודולים הלא-מחוברים** — `BookingRepository` נושא
   מימוש **יחיד**, `MemoryRepository`, והוא יושב בתוך קובץ המבחן
   (`operations.test.ts:68`). אין מתאם Supabase לאף אחד מ-booking, metrics
   או preparation, ואף מסך אינו מייבא אותם. 1718 המבחנים הירוקים מוכיחים את
   הלוגיקה בבידוד — הם **אינם** מוכיחים שהזמנה ניתנת ליצירה במוצר. שכבת
   `actor` לעומת זאת כן מחוברת (`SupabaseActorSource`).
4. **E2E ודפדפן** — אין מנגנון. `OPEN_GAPS` G-004 פתוח ובצדק.
5. **הצלבת `FEATURE_MATRIX.md` ו-`DECISION_LEDGER.md` מול העץ** — הופקדה
   לסוכן משנה שנקטע במגבלת ההפעלה ולא החזיר תוצאה. **לא בוצעה.** זהו הפער
   המשמעותי היחיד בכיסוי הביקורת הזו, ואיתור אובדן פיצ'רים שקט נותר פתוח.
6. **המצב רגעי.** בין תחילת הביקורת לסופה נוספו `agents/`, `finance/` ו-4
   מיגרציות, ומודולים שנצפו כיתומים בשעה אחת נצרכו כהלכה בשעה שאחריה
   (`contracts/` היה ללא צרכנים ב-23:31 והיו לו 7 ב-04:06). כל מספר כאן נכון
   לחותמת הזמן שלו ולא אחריה.

---

## סיכום לפי חומרה

| חומרה | מספר | מזהים |
|---|---|---|
| CRITICAL | 1 | F-01 |
| HIGH | 3 | F-02, F-03, F-04 |
| MEDIUM | 7 | F-05, F-06, F-07, F-08, F-09, F-10, F-11 |
| LOW | 3 | F-12, F-13, F-14 |
| **סה"כ** | **14** | |

מתוכם **אחד כבר נכתב לו תיקון** (F-02, במיגרציה 0012) הממתין להחלה,
ואחד **בתיקון פעיל** (F-09).

**סדר טיפול מומלץ:** F-01 תחילה — הוא זול לתקן (מחיקת שורה אחת ותיקון מה
שהטייפצ'ק יאיר), והוא מונע את הצטברות ההמצאות ב-15 המקומות מלהפוך ל-30
בשבוע הבא. אחריו F-02, שהוא היחיד עם השלכת פרטיות חיה. F-08 דורש תיאום
ולא ביצוע חד-צדדי.
