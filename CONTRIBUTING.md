# עבודה על ESTIA

מסמך קצר: איך מתקינים, איך מריצים, ומה בודקים לפני כל commit.
המסמך מיועד גם למי שאינו מתכנת — כל פקודה מוסברת במילים פשוטות.

---

## התקנה חד-פעמית

צריך Node.js בגרסה שמוגדרת בקובץ `.nvmrc` (כרגע 24.15.0).
מי שמשתמש ב-nvm יכול פשוט להריץ בתיקיית הפרויקט:

```bash
nvm use
```

ואז, להתקנת הספריות:

```bash
npm ci
```

> `npm ci` ולא `npm install` — הפקודה הזו מתקינה בדיוק את הגרסאות שרשומות
> ב-`package-lock.json`, ולכן היא מביאה אצל כולם בדיוק את אותה סביבה.

### משתני סביבה

מעתיקים את `.env.example` לקובץ `.env.local` וממלאים את הערכים:

```bash
cp .env.example .env.local
```

`.env.local` **לעולם לא נכנס ל-git**. המפתח מסוג `service role` עוקף את כל
הגנות בסיס הנתונים, ולכן הוא נמצא רק בכספת הסודות — אף פעם לא בקוד ולא במאגר.

---

## הרצה מקומית

```bash
npm run dev
```

והדפדפן נפתח ב-<http://localhost:3000>.

> **שימו לב:** בפרויקט עובדים כמה אנשים במקביל, והפורטים **3000, 3100 ו-3200**
> עשויים להיות תפוסים על ידי שרת פיתוח של מישהו אחר. אם הפורט תפוס, Next.js
> יבחר פורט פנוי אחר ויכתוב אותו במסוף — פשוט פותחים את הכתובת שהוא הדפיס.
> אין צורך "להרוג" את השרת של אף אחד.

---

## לפני כל commit — שער האיכות

פקודה אחת מריצה את הכל:

```bash
npm run verify
```

היא מריצה שישה שלבים, לפי הסדר. אם שלב נכשל, הריצה נעצרת שם.

| השלב          | הפקודה                   | מה זה בודק, במילים פשוטות                                                                         |
| ------------- | ------------------------ | ------------------------------------------------------------------------------------------------- |
| עיצוב קוד     | `npm run format:check`   | שכל הקבצים כתובים באותו סגנון — רווחים, מרכאות, ירידות שורה. לא משנה את ההתנהגות, רק את הקריאוּת. |
| בדיקת תקינות  | `npm run lint`           | מאתר טעויות נפוצות ודפוסים מסוכנים, למשל קישור שנכתב בצורה שפוגעת בניווט באתר.                    |
| בדיקת טיפוסים | `npm run typecheck`      | מוודא שהקוד מדבר עם עצמו נכון — ששדה שהוגדר כמספר לא מקבל טקסט. תופס באגים לפני שהם מגיעים למסך.  |
| בדיקות יחידה  | `npm run test`           | מריץ את כל הבדיקות האוטומטיות של לוגיקת המוצר: הרשאות, חבילות, מכסות. לא נדרש בסיס נתונים.        |
| בדיקות אבטחה  | `npm run security:check` | מוודא שלא נכנס למאגר מפתח, כתובת של פרויקט Supabase, או קובץ `.env`.                              |
| בנייה         | `npm run build`          | בונה את גרסת הייצור. אם זה עובר — האתר ניתן לפריסה.                                               |

פקודות נוספות:

```bash
npm run test:watch      # בדיקות שרצות מחדש עם כל שמירה, בזמן פיתוח
npm run test:coverage   # אחוז הקוד שמכוסה בבדיקות
npm run format          # מתקן בפועל את סגנון הקוד (ולא רק בודק)
```

---

## הכלל שאין לעקוף

**שער שנכשל לעולם אינו נעקף.**

לא מוסיפים `--no-verify`, לא מבטלים בדיקה, ולא מורידים סף כיסוי כדי שהבנייה
תהיה ירוקה. בדיקה אדומה היא מידע — היא אומרת שמשהו נשבר, ולפעמים היא הדבר
היחיד שעומד בין באג לבין לקוח אמיתי.

כשבדיקה נכשלת, קודם כול מבינים למה:

1. **פגם במוצר** — מתקנים את הקוד.
2. **פגם בבדיקה** — מתקנים את הבדיקה, ומסבירים ב-commit מדוע היא הייתה שגויה.
3. **בעיית סביבה** — למשל ספרייה חסרה. מתקנים את הסביבה.

מה שלא עושים: משנים את הבדיקה כדי שתעבור, או מריצים שוב ושוב עד שבמקרה יוצא ירוק.

אם השבירה אינה שלך — למשל קובץ שעמית עדיין עובד עליו — **לא מתקנים את הקוד שלו**.
מדווחים לו. בפרויקט עובדים כמה מפתחים במקביל, וכל אחד אחראי לקבצים שלו.

---

## סגנון קוד

הסגנון נאכף על ידי Prettier ומוגדר ב-`.prettierrc.json`: בלי נקודה-פסיק בסוף
שורה, מרכאות בודדות, ורוחב שורה של 80 תווים. אין צורך לזכור — `npm run format`
עושה את זה.

> **הערה למי שיריץ את זה בפעם הראשונה:** נכון לכתיבת המסמך, המאגר עדיין לא עבר
> פורמט מלא, ולכן `npm run format:check` מדווח על קבצים רבים. ההרצה הראשונה של
> `npm run format` צריכה להיעשות ב-commit נפרד ומתואם, שנוגע _רק_ בעיצוב — אחרת
> היא תתנגש בעבודה של כל מי שפתוח על הקבצים האלה כרגע.
